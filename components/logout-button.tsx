import { signOut } from "next-auth/react";
import { Button } from "./ui/button";

const LogoutButton = () => {

    return ( 
        <div onClick={() => {}}>
            logout
        </div>
     );
}
 
export default LogoutButton;